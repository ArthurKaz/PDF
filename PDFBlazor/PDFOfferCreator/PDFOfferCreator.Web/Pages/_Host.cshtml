@page "/"
@namespace PDFOfferCreator.Web.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "_Layout";
}

<component type="typeof(App)" render-mode="ServerPrerendered" />

<script>
    function showAlert(message) {
        var element = document.getElementById("p-container");
        var newElement = document.createElement("p");
        newElement.innerHTML = "Appended paragraph"
        element.append(newElement);
        return alert(message);


    }
    function showDialog(fileName) {
        const blob = new Blob([pdfData], { type: 'application/pdf' });

        if (navigator.msSaveBlob) {
            // Для Internet Explorer
            navigator.msSaveBlob(blob, fileName);
        } else {
            // Для сучасних браузерів
            const link = document.createElement('a');
            link.href = URL.createObjectURL(blob);
            link.download = fileName;
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        }
       /* const data = 'Це текст, який ви хочете зберегти в файлі.';
        const blob = new Blob([data], { type: 'text/plain' });
        if (navigator.msSaveBlob) {
            // Для Internet Explorer
            navigator.msSaveBlob(blob, fileName);
        } else {
            
            const link = document.createElement('a');
            link.href = URL.createObjectURL(blob);
            link.download = fileName;
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        }*/
    }
</script>
